{
  "name": "eyebrowse-web-test",
  "private": true,
  "type": "module",
  "description": "Headless-browser test harness for the eyebrowse WebGPU runtime",
  "scripts": {
    "browser-test": "node scripts/browser-test.mjs"
  },
  "dependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
